V10:
1)对于AI，射击判定当子弹消亡之后还需要冷却400ms才能发射子弹（防止玩家和AI对波对不赢AI）
2）碰撞判定修改，由原来的<45改成新的逻辑判定，如果对方坦克是整点且距离小于等于40判定为碰撞，如果是非整点先判定当前坐标是否小于40，然后可以求出对齐之后的坐标，再判断对齐后坐标是否小于等于40，如果是则判定为碰撞
3)将Tank类一些和类无关的函数设成final static，增加tank类新写函数的注释